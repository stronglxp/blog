# database configurations
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/blog?characterEncoding=utf-8&useSSL=false&useAffectedRows=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Baby3344!
#spring.datasource.password=1311664842
spring.datasource.tomcat.connection-properties=useUnicode=true;characterEncoding=utf-8;

# mybatis configurations
mybatis.config-location=classpath:mybatis-config.xml
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.codeliu.blog.entity

# change the port
server.port=8081

# thymeleaf configurations, local test
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.mode=HTML

# Spring boot takes static as the default folder,
# but does not know the subfolders under static.
# In this way, 404 will appear when accessing static resources
spring.mvc.static-path-pattern=/static/**

server.max-http-header-size=10000000